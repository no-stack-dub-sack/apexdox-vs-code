public class TEST_ExcludeMe {
    // Without the 'excludes' setting, this class would be included in the documentation
    // because the 'includes' settings incldes all files that start with 'TEST_'. However
    // this file should be excluded since it's been specifically called out in 'excludes'
}